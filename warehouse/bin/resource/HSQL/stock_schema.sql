DROP TABLE STOCK if exists
CREATE TABLE STOCK(ID INTEGER IDENTITY PRIMARY KEY,GOODS_CODE INTEGER,QUANTITY INTEGER, STATUS VARCHAR(10) NOT NULL)
ALTER TABLE STOCK ADD CONSTRAINT UNQSTOCK UNIQUE(GOODS_CODE)